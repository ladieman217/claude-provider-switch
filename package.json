{
  "name": "claude-provider-switch-workspace",
  "private": true,
  "scripts": {
    "dev": "nodemon --watch packages --ext ts,tsx,css,html,json --signal SIGTERM --exec \"pnpm run dev:inner\"",
    "dev:inner": "concurrently \"pnpm -C packages/ui run dev\" \"pnpm -C packages/cli run dev\"",
    "build": "pnpm -C packages/cli run build",
    "lint": "pnpm -C packages/cli run lint && pnpm -C packages/ui run lint",
    "test": "pnpm -C packages/cli run test",
    "release:check": "pnpm run lint && pnpm run test && (cd packages/cli && npm_config_cache=../../.npm-cache npm pack --dry-run)",
    "release:publish": "cd packages/cli && npm_config_cache=../../.npm-cache npm publish --access public",
    "release:patch": "bash scripts/release-bump.sh patch",
    "release:minor": "bash scripts/release-bump.sh minor",
    "release:major": "bash scripts/release-bump.sh major"
  },
  "devDependencies": {
    "concurrently": "^9.1.0",
    "nodemon": "^3.1.7",
    "typescript": "^5.6.3"
  }
}
